import { LoginComponent } from './login.component';


describe('LoginComponent', () => {

  beforeEach(() => loginComponent = new LoginComponent() );

  it('should exist', () => {
    expect(loginComponent).toBeTruthy();
  });

  it('O campo usuário deve ter apenas letras', () => {

  });
});
